# App
NODE_ENV=development
PORT=3001
APP_NAME=MediStock

# Docker Database Configuration
POSTGRES_USER=medistock
POSTGRES_PASSWORD=medistock123
POSTGRES_DB=medistock
POSTGRES_PORT=5432
POSTGRES_HOST=localhost

# Database URL (for production or migration commands)
# Constructed as: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
DATABASE_URL=postgresql://medistock:medistock123@localhost:5432/medistock

# Docker Redis Configuration
REDIS_PORT=6379

# JWT
JWT_SECRET=your-super-secret-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS
ALLOWED_ORIGINS=http://localhost:3000